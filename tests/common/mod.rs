//! Shared test-harness utilities for integration tests.
//!
//! Modules are declared here so `tests/<name>.rs` files can pull them in
//! with `mod common;` (Cargo's integration-test convention).

pub mod opencode_harness;
